<Window x:Class="MVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Button x:Name="button" Content="Button" HorizontalAlignment="Left" Margin="233,208,0,0" VerticalAlignment="Top" Width="75" Command="{Binding AddCommand}" />
        <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="23" Margin="211,96,0,0" TextWrapping="Wrap" Text="{Binding Input2}"  VerticalAlignment="Top" Width="120"  />
        <TextBox x:Name="textBox1" HorizontalAlignment="Left" Height="23" Margin="211,42,0,0" TextWrapping="Wrap" Text="{Binding Input1}" VerticalAlignment="Top" Width="120"/>
        <TextBox x:Name="textBox2" HorizontalAlignment="Left" Height="23" Margin="211,155,0,0" TextWrapping="Wrap" Text="{Binding Result}" VerticalAlignment="Top" Width="120"/>
        <Rectangle  HorizontalAlignment="Left" Height="100" Margin="33,170,0,0" Stroke="Black" VerticalAlignment="Top" Width="100" Name="result" Fill="{Binding Result_color}"/>

    </Grid>
</Window>
